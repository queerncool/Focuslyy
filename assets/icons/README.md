# App icon source files

Per the brand system (CLAUDE.md): the icon is an **italic lowercase "y" in amber
(`#F2A03D`) on espresso (`#18120E`)** — the only letter in the wordmark with a
descender, "below the surface, where deep work happens."

Drop the source SVG(s) and the rendered `icon-1024.png` here.

**Phase 8 expects:**

- `icon-1024.png` — 1024×1024, re-rendered with real **Fraunces Black Italic**
  loaded (the typeface is already in `assets/fonts/Fraunces-BlackItalic.ttf`).

Until the founder-approved source art lands, `app.json` points at the placeholder
`assets/images/icon.png` so the app still builds.
