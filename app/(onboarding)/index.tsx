import { PlaceholderScreen } from '@/components/PlaceholderScreen';

/** Placeholder until the Phase-2 funnel (Start → … → FirstSeal) is built. */
export default function OnboardingPlaceholder() {
  return <PlaceholderScreen title="Onboarding" kicker="60 seconds" />;
}
