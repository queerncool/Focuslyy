import { PlaceholderScreen } from '@/components/PlaceholderScreen';

/** Placeholder until the Phase-3 session loop is built. */
export default function SessionPlaceholder() {
  return <PlaceholderScreen title="Session" kicker="lock in" />;
}
